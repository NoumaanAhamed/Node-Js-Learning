const math = require("./math");
// const {add,sub} = require('./math')

//* require('something') --> check from installed modules/built-in packages
//* require('./something') --> check from present files in current directory

console.log(math.add(2, 5));
console.log(math.sub(2, 5));
